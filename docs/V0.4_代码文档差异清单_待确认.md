# ArchaeoPairs 代码/README vs 《技术方案 V0.4》 差异清单（待确认）

> 对比对象：`src/`、`tests/`、`README.md`、`CHANGELOG.md`、`pyproject.toml`、`config/*.example.yaml`、`ddl.sql`、`.github/workflows/` vs `docs/考古报告图文Pair数据构造_多智能体方案_V0.4.docx`（含 `V0.4_修订纪要_B2_C1.md`、`评审意见_多智能体方案_V0.4.md`）
> 生成时间：2026-08-18。状态：**已按确认全部执行**（2026-08-18，见文末"执行结果"）。

---

## ✅ 执行结果（2026-08-18，全部通过门禁：pytest 144 / ruff / mypy）

| 项 | 执行 |
|---|---|
| 1-5 版本类 | ✅ README 依据改 V0.4；pyproject 0.4.0 + 描述 V0.4；CHANGELOG 增补 0.4.0；6 个代码文件头注释 + ddl.sql → V0.4；README License 统一 Apache-2.0 |
| 6 README mermaid | ✅ 替换为 V0.4 附录 B 三线路（S2→单器物线/彩→S7；多器物线→S3~S6；多器物彩/其它→归档；S7→S8→S9→S10） |
| 8-9 乱码 docstring | ✅ 修复 route_single 与 _artifact_signals 乱码 |
| 10-11 跨图合并 | ✅ 删除 s8.merge_pair_records 及测试、cli.py 汇总调用、PipelineFlags.cross_fig_merge、flags.example.yaml 项 |
| 12 成本帽/熔断 | ✅ gateway 去 CostCapExceeded/熔断/成本累计；nodes 去 cost_cap 分支；Thresholds.per_figure_cap_cny 删（保留 model_costs 单价）；thresholds.example.yaml 同步 |
| 13-14 DDL | ✅ ddl.sql 注释 V0.4；新增 migrations/V001__init.sql（Flyway/Liquibase 风格） |
| 16-19 文档落笔 | ✅ V0.4.docx 补 §4.1 C1 措辞、§4.2 B2 措辞、修订历史 V0.4.1 行、§7.2 命名示例行 |
| 20-21 清理 | ✅ 无残留引用（grep 验证） |

**未执行项说明**：清单第 15 项（术语统一 seq_id 的 schema 迁移）——核验 V0.4 §3.4.2 State Schema 文档本身使用 `seq_annotations`/`SeqAnnotation`/`NoteItem` 字段名，与代码**已一致**，无需迁移；`split_same_seq`/`seq_missing` 等枚举名文档同样保留。

---

## 一、版本与文档依据类（低风险，建议直接更新）

| # | 位置 | V0.4 要求 / 现状 | 差异 | 建议 |
|---|---|---|---|---|
| 1 | `README.md` 首段 | 依据声明写"《…方案 V0.3》" | V0.4 已发布（三线路架构、S7/S8 更名） | 改为 V0.4 |
| 2 | `pyproject.toml` `version=0.2.1`、description"依据《技术方案 V0.3》" | 文档已到 V0.4 | 版本号/依据未对齐 | 版本 bump（如 0.4.0）并改描述 |
| 3 | `CHANGELOG.md` | 最高 0.2.1（2026-08-17） | 未记录 V0.4 对应变更（三线路/B2/C1/01占位） | 增补版本条目 |
| 4 | 代码头部注释引用 V0.3：`state.py`/`naming.py`/`capability/base.py`/`parsers/s1_xml.py`/`config/settings.py`/`storage/db.py`/`ddl.sql` | "对齐《技术方案 V0.3》§x.x" | V0.4 已发布，引用滞后 | 统一改为 V0.4（或去版本号写"技术方案"） |
| 5 | `LICENSE` vs `pyproject.toml` | pyproject 声明 Apache-2.0；README 写"MIT 占位，正式在 Apache-2.0/MIT 间确认" | 两处不一致 | 定论 License 并同步三处 |

---

## 二、架构 / 路由差异（核心，需确认）

| # | 位置 | V0.4 要求 | 现状 | 差异 |
|---|---|---|---|---|
| 6 | `README.md` mermaid 图 | 附录 B 三线路：`S2→单器物线/彩→S7 单器物解析器`；`S2→多器物线图→S3~S6`；`S2→多器物彩图/其它→归档`；`S7→S8→S9`，`S9→S10` | 仍是旧路由：`S2→\|line_drawing\|→S3/S4`、`S2→\|plate\|→S7[s7 彩板]`、`S7→S9`、`S9→\|case_group_error\|→S8` | **README 图与 V0.4 附录 B 完全不符**，且 S7 仍叫"彩板" |
| 7 | `src/archaeopairs/orchestration/routing.py` `route_classify` | 三线路分流 | `single_line/plate_artifact→parse_single`；`multi_line/line_drawing→parse_text+parse_image`；其它→END | ✅ 与 V0.4 一致 |
| 8 | `routing.py` `route_single` docstring | — | `"""??????V0.3??EXCLUDED ?????PENDING ..."""` **乱码** | 编码损坏，需修复注释 |
| 9 | `parsers/keywords.py` `_artifact_signals` docstring | — | `"""???? XML ??????????..."""` **乱码** | 编码损坏，需修复注释 |

---

## 三、范围差异（V0.4 已移除，代码仍残留 —— 需决策）

### 3.1 跨图合并
| # | 位置 | V0.4 要求 | 现状 |
|---|---|---|---|
| 10 | `src/archaeopairs/agents/s8.py` `merge_pair_records` | §4.8.2/附录B：**跨图合并不在 V0.4 范围**（按单图独立输出，不聚合候选） | 函数仍在，且 `cli.py:105` 仍调用、`tests/test_s8_assemble.py:76` 仍测试；s8.py 注释还写"V0.3 .4.8.2 cross-figure merge" |
| 11 | `config/flags.example.yaml` `cross_fig_merge: false`；`state.py` `PipelineFlags.cross_fig_merge` | 已移除 | Flag 仍保留 |

### 3.2 成本帽 / 熔断
| # | 位置 | V0.4 要求 | 现状 |
|---|---|---|---|
| 12 | `gateway.py`（`per_figure_cap_cny`/circuit/CostCapExceeded）、`config/settings.py` `Thresholds.per_figure_cap_cny`/`model_costs`、`config/thresholds.example.yaml`、`nodes.py` CostCapExceeded 分支 | V0.4 正文仅 §3.7 服务级降级，**无成本帽/熔断**（历史表 T0 才提及） | 代码仍完整实现成本帽与熔断 |

> 决策点 A：成本帽/熔断是**删除代码**，还是**保留为超集**（文档补回 §5.9）？评审意见维度 G 确认"移除成本帽/熔断是正确收敛"。

---

## 四、DDL / 数据库差异

| # | 位置 | V0.4 要求 | 现状 | 差异 |
|---|---|---|---|---|
| 13 | `ddl.sql` 头部注释 | V0.4 §6.5 存在（PostgreSQL 四表 + 幂等键/状态查询/JSONB GIN 索引） | 注释写"对齐《技术方案 V0.3》§6.5" | 版本引用滞后；表结构本身与 V0.4 四表基本一致 ✅ |
| 14 | DDL 迁移管理 | V0.4 §6.5.1："DDL 经 Flyway/Liquibase 迁移管理，V001__init.sql 起" | 仓库仅 `ddl.sql` 单文件，无迁移框架 | 无迁移脚本目录（migrations/V001__init.sql） |

---

## 五、术语差异（此前已统一文档，代码未动）

| # | 位置 | V0.4 要求 | 现状 |
|---|---|---|---|
| 15 | 文档 §1.6/全文 | 图注序号统一 `seq_id` | `state.py`：`NoteItem.seq`、`SeqAnnotation`、`seq_annotations`、`split_same_seq`、`MaskRecord.seq` 等仍为 `seq` |

> 上次已确认：复合契约标识符不改（与 codebase 对应）。如要连字段名一起统一为 `seq_id`，属代码 schema 迁移，需另行决策。

---

## 六、文档侧未落笔项（V0.4 修订纪要遗留，需确认是否补齐）

| # | 位置 | 修订纪要要求（§三） | 现状 |
|---|---|---|---|
| 16 | V0.4 §4.2 | 补 B2 措辞："单/多判定以 XML 器物号为一级证据…rule_b 多视图亦有多个序号，不得仅凭序号个数判定" | §4.2 正文无此句（决策表 T18 已隐含） |
| 17 | V0.4 §2.4 / §4.1 | 补 C1 措辞："同图号连续 figure 分组 + 组内面积最大归属 + 图注前置回溯 + 组后多段合并 + 相邻 figure-title 恢复" | §4.1 仅一句职责，未展开 |
| 18 | V0.4 修订历史 | 增补 V0.4.1 行 | 现仅有 V0.4 行，无 V0.4.1 |
| 19 | V0.4 §7.2 | 补多器物路径示例 + rule_b 整图亦用 01 说明 | 命名表已有 `{原图名}_[seq_id\|01]_{artifact_id}.png`，示例是否补全待确认 |

---

## 七、代码质量 / 待清理

| # | 位置 | 问题 |
|---|---|---|
| 20 | `routing.py`、`keywords.py` docstring 乱码 | 中文注释编码损坏（显示 `????`），需修复 |
| 21 | `s8.py` `merge_pair_records` + `cli.py` 调用 + 对应测试 | 若确认移除跨图合并，需删除函数/调用/测试，避免死代码 |

---

## 八、已对齐项（无需动作）

- ✅ S7/S8 更名：文档 §4.7/§4.8 与代码 `s7.py`（single-artifact parser）/`s8.py`（单器物组装）语义一致
- ✅ S2 单/多判定以 XML 器物号为一级证据（B2）：`keywords.refine_image_type` 已实现，与修订纪要一致
- ✅ S1 图注关联（C1）：`s1_xml.py` 同题分组/前置回溯/面积归属/相邻恢复已实现
- ✅ 单器物命名 01 占位：`naming.build_image_name` 已实现
- ✅ 三线路 graph 拓扑：`graph.py`/`nodes.py`/`routing.py` 与 V0.4 一致
- ✅ 降级矩阵置信：`config/thresholds.example.yaml` 与 V0.4 §3.6 一致
- ✅ CI：ruff/mypy/coverage≥80 与 V0.4 §10.4 一致

---

## 待确认决策项汇总

1. **A. 版本号**：代码/CHANGELOG bump 到与 V0.4 对应的版本（如 0.4.0）？
2. **B. 成本帽/熔断**：删除代码残留，还是保留并在文档补回？
3. **C. 跨图合并**：删除 `merge_pair_records`/flag/测试，还是保留？
4. **D. DDL 迁移**：是否补 Flyway/Liquibase 迁移目录（V001__init.sql）？
5. **E. 文档落笔**：是否把修订纪要 §三 的 4 个落笔点（B2/C1/历史/命名）补进 V0.4.docx？
6. **F. 乱码注释**：修复 routing.py / keywords.py 的乱码 docstring？
7. **G. README**：mermaid 与依据版本是否按 V0.4 附录 B 更新？
8. **H. 术语**：代码字段名是否随文档统一为 seq_id（schema 迁移）？
