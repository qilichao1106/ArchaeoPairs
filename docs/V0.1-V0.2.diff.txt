=> | V0.2 | 修订 S9 必选终检/迭代开关、统一幂等键、Pair 多候选 Schema、SAM 文字比例尺掩膜合并、正文切分默认 LLM 确认；修正 21 份 XML 数据口径与资源估算。 | 2026-08-13 | 齐利超 |  |
=> 本节描述的数据特征基于 21 份考古报告 XML 实测抽象（examples 全量、非抽样），仅作为样本反映考古报告领域的典型形态分布；方案必须能泛化到未纳入样本的报告。实测规模：16,124 个 imagedata、media 约 2.4GB（压缩 jpg，平均约 143KB/张）。
=> 量化口径与来源（E3）：全文量化数字以 21 份报告 XML 实测为准（图数 16,124 imagedata、media≈2.4GB、单图≈143KB），来源为本文 §3.2 数据规模验证与 V0.3 评审报告附录 A 实测统计；如与早期版本估算冲突，以实测为准。
=> 单一全局 PairState 承载跨节点运行时状态；FigureState 是持久化聚合状态，PairState 由 FigureState 转换而来。为避免双状态漂移，所有新增字段必须先落到 FigureState，再通过 to_pair_state()/from_pair_state() 同步；子模型字段级定义见 §6.1。
=> thread_id（checkpointer） = book_id:figure_id:rule_version:prompt_version:judge_prompt_version，不含 iteration；轮次级调用幂等键 = thread_id:iteration。P0 用 SqliteSaver（零依赖、单文件、易调试）；生产切 PostgresSaver 复用现有 PG（切换点为创建 checkpointer 的工厂函数）。与 §5.8 幂等、§9.3 任务认领分工：claim（PG SELECT FOR UPDATE SKIP LOCKED）决定“哪个 Worker 跑该 figure”，checkpointer 决定“中断后从最近 State 续跑”。
=> | 输入 | Path: data.xml; Format: DocBook 5.0; Schema: archbook.xsd（上游交付项；未入库前用 XML 结构断言作为临时契约校验） |
=> ④ 低置信段 LLM 兜底（s3_llm_confirm 默认开启，仅允许在 golden set 回归通过后关闭）
=> 归组判定与执行收敛于 S8：S8 一次性向 S9 请求归组证据并据此执行合并；证据不足直接转 PENDING_REVIEW，不做 S8/S9 多轮往返。跨图合并（line_drawing+plate 同 PairRecord）受 Feature Flag cross_fig_merge 控制，P0 默认关闭；关闭时仍保留同一 artifact_id 的全部候选图，按 §4.8.2 选主图并写入 candidate_images，不因唯一键冲突丢图。
=> 合并维度：line_drawing（mask 子图）+ plate（单张彩图）合并为同一 PairRecord；未开启 cross_fig_merge 时，线图优先为主图，plate 存为候选图。
=> 多候选保留：同一 artifact_id 在不同图/图版出现时，选择主图写入 image_path，其余候选写入 candidate_images，记录 source_figure_id、role、confidence；不删除候选。
=> 职责：对 Worker 产出结构化诊断，产出 DiagnosticReport，驱动针对性二次修正。S9 分为必选终检与可选迭代：每个进入 S9 的 figure 至少执行 1 次终检；当 s9_loop 开启时，按 §4.9.2 进入最多 3 轮修正回环。
=> 说明文字与比例尺并入掩膜的算法：OCR 文本区域先转 mask，再与所属线图 mask 做像素级 union，最后与专属比例尺 mask union；跨器物文字或比例尺先按 seq 归属，再合并，禁止借用非自身区域。SAM 对密集线图或共享基准线失败时，降级使用轮廓闭合+连通域候选；仍无法满足闭合约束则触发 mask_incomplete。
=> 整段无信号则挂最近上文锚点并标记 no_anchor，进入强制 LLM 确认。
=> 低置信段（无锚点归并）必须送 LLM 二次确认，确认依据写入 markers；s3_llm_confirm 默认开启，关闭前须通过 golden set 回归门禁。
=> 幂等键分两层：checkpointer 线程键 = book_id:figure_id:rule_version:prompt_version:judge_prompt_version；单轮调用/产物键 = 线程键:iteration。
=> | S9 Supervisor VLM | 1（必选终检） | 3（迭代 × 3 节点） | 5k |
=> | 总计 | ≈4.5 | ≈12 | — |
=> 以 21 份示例 XML 实测 16,124 imagedata、media 约 2.4GB 为基准
=> P0 POC 阶段：建议档位 B + 3 卡 GPU；VLM 全量约 4.3 小时，SAM 约 10.5 小时，端到端含 IO/人工复核按 15–25 小时排期。
=>     book_id: str; artifact_id: str; image_path: str  # primary image    candidate_images: list[ImageRef] = []  # same artifact_id across figures/plates    image_merge_mode: Literal["line_only","plate_only","line_plus_plate","multi_candidate"] = "line_only"
=> pair_records：UNIQUE(book_id, artifact_id) 承载逻辑键，candidate_images JSONB 保留同 artifact_id 的多候选图；review_tasks：event_id UNIQUE 承载 webhook 幂等去重。
=>   candidate_images JSONB NOT NULL DEFAULT '[]'::jsonb, image_merge_mode TEXT NOT NULL DEFAULT 'line_only',
=> Feature Flag 清单（P0 默认：s3_llm_confirm=true，s9_loop=true，cross_fig_merge=false，rotation_correct=true；均可独立开关以 A/B 对比）：s3_llm_confirm（S3 低置信 LLM 二次确认）、s9_loop（S9 Supervisor 闭环）、cross_fig_merge（跨图合并）、rotation_correct（纵横向旋转校正）。硬约束不在此清单、不可关。
=> 按 §5.9 调用预算：16,124 图 × 单图最坏 12 次模型调用 = 193,488 次调用（其中 VLM 按 §5.9.1 单图预算核算）。
=> 存储：原图实测约 2.4GB（压缩 jpg）；产物按 3× ≈ 7.2GB；中间 mask 按 5× ≈ 12GB（复核通过+7 天清理）。P0 可用本地磁盘/MinIO 承载，S3 10TB+ 为生产冗余预算。
<= 本节描述的数据特征基于22 份考古报告实测抽象（examples 全量、非抽样），仅作为样本反映考古报告领域的典型形态分布；方案必须能泛化到未纳入样本的报告。实测规模：约16,124 个 imagedata 标签、media 全量约2.8GB（压缩 jpg，均约160KB/张）。
<= 量化口径与来源（E3）：全文量化数字以 22 份报告实测为准（图数 16,124 imagedata、原图≈2.8GB、单图≈160KB），来源为本文 §3.2 数据规模验证与 V0.3 评审报告附录 A 实测统计；如与早期版本估算冲突，以实测为准。
<= 单一全局 PairState 承载跨节点共享字段；子模型（NoteItem/TextArtifact/SeqAnnotation/ScaleAnnotation/FusedMapping/MaskRecord/DiagnosticReport/PairRecord/PipelineFlags）字段级定义见 §6.1。
<= thread_id = 幂等键 book_id:figure_id:rule_version:prompt_version:judge_prompt_version。P0 用 SqliteSaver（零依赖、单文件、易调试）；生产切 PostgresSaver 复用现有 PG（切换点为创建 checkpointer 的工厂函数）。与 §5.8 幂等、§9.3 任务认领分工：claim（PG SELECT FOR UPDATE SKIP LOCKED）决定"哪个 Worker 跑该 figure"，checkpointer 决定"中断后从最近 State 续跑"。
<= | 输入 | Path: data.xml; Format: DocBook 5.0; Schema: archbook.xsd |
<= 对赵廷隐(92%)、栎阳城(98%)、明楚昭王(95%) 等链①大面积缺失的报告，S3 启用链②反推增强规则：① 扩展锚点正则覆盖 Bb9/Zhb2/406号墓/C5.1H146；② 视图名共现（"盖顶面纹饰"+"颈部"→同器）辅助归并；③ 标本号/件数语/图引用三重信号交叉；④ 低置信段 LLM 兜底（受 s3_llm_confirm 开关控制）；⑤ 链②单源置信封顶 0.60，链②+链③封顶 0.70（§3.6），低于输出门槛转 PENDING_REVIEW。
<= 归组判定与执行收敛于 S8：S8 一次性向 S9 请求归组证据并据此执行合并；证据不足直接转 PENDING_REVIEW，不做 S8/S9 多轮往返。跨图合并（line_drawing+plate 同 PairRecord）为可选增强，受 Feature Flag cross_fig_merge 控制，P0 默认关闭，待确认下游训练确需"一个 Pair 同时含线图+彩板"后再开启。
<= 合并维度：line_drawing（mask 子图）+ plate（单张彩图）合并为同一 PairRecord。
<= 职责：对 Worker 产出结构化诊断，产出 DiagnosticReport，驱动针对性二次修正。
<= 无锚点段：按优先级归并——检测件数语（"1件。"）归并最近上文锚点；检测标本号（"标本M140:5"）归并对应锚点；检测图引用（"（图5.85、图5.86）"）保留为 provenance；整段无信号则挂最近上文锚点。
<= 低置信段（无锚点归并）送 LLM 二次确认，确认依据写入 markers。
<= 幂等键：book_id:figure_id:rule_version:prompt_version:judge_prompt_version:iteration
<= | S9 Supervisor VLM | 0 | 3（迭代 × 3 节点） | 5k |
<= | 总计 | ≈2.5 | ≈12 | — |
<= 以 22 份样本报告实测约16,124 imagedata 为基准
<= P0 POC 阶段：建议档位 B + 3 卡 GPU，预算 6 小时/全量
<=     book_id: str; artifact_id: str; image_path: str
<= pair_records：UNIQUE(book_id, artifact_id) 承载逻辑键；review_tasks：event_id UNIQUE 承载 webhook 幂等去重。
<= CREATE TABLE review_tasks(
<= Feature Flag 清单（P0 可独立开关以 A/B 对比）：s3_llm_confirm（S3 低置信 LLM 二次确认）、s9_loop（S9 Supervisor 闭环）、cross_fig_merge（跨图合并）、rotation_correct（纵横向旋转校正）。硬约束不在此清单、不可关。
<= 按 §5.9 调用预算：16,124 图 × 单图最坏 12 次 VLM = 186,432 次调用。
<= 存储：原图实测约2.8GB（压缩 jpg）；产物按 3× ≈ 8.4GB；中间 mask 按 5× ≈ 14GB（复核通过+7 天清理）。P0 可用本地磁盘/MinIO 承载，S3 10TB+ 为生产冗余预算。
